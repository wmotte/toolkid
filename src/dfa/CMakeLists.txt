project( dfa )

include_directories( ${Boost_INCLUDE_DIRS} )

if( Boost_FOUND )
	
  # detrended fluctuation analysis (dfa)
   #	add_executable( dfa dfa.cpp )
  #	target_link_libraries( dfa ${Boost_LIBRARIES} )

endif( Boost_FOUND )

